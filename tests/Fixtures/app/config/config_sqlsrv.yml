imports:
    - { resource: config.yml }

doctrine:
    dbal:
        path: ~
        dbname: master

    orm:
        auto_generate_proxy_classes:   '%kernel.debug%'
        auto_mapping:                  true

